[package]
name = "canopus-host-fake"
description = "Host-only fake target for Rust modules — allocator, clock, timer wheel, driver namespace, module harness (mirror of tests/host/fake_target.c)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version = "1.85"

[dependencies]
canopus-abi = { workspace = true }
canopus-runtime = { workspace = true }
